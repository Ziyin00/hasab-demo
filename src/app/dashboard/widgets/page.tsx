import { ChatbotWidgetsPage } from "@/features/chatbot-widgets/components/ChatbotWidgetsPage";

export const metadata = { title: "Widgets — Hasab AI" };

export default function Page() {
  return <ChatbotWidgetsPage />;
}
